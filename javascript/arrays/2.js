let emp = { id: 101, name: "Rahul" }
//let k = Object.keys(emp) //[id,name]
//console.log(k.length > 0)
if (Object.keys(emp).length > 0) {
    console.log("Not Empty")
} else {
    console.log("Empyt Object")
}

